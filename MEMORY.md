# MEMORY.md - 长期记忆

## 🤖 核心运作规则

### 定时任务执行策略（2026-03-12 确立）

**核心原则：根据时间明确性选择工具**

| 场景 | 关键词/表述 | 使用工具 | 存储位置 |
|------|------------|---------|---------|
| **明确定时任务** | "每天 X 点提醒我"、"每周一 X 点"、"X 分钟后通知我"、"X 月 X 日提醒我" | ✅ **feishu-reminder** | OpenClaw Cron |
| **待办事项** | "代办"、"待办"、"要做的事"、"记得做 X"（无明确时间） | ✅ **FlashMemo** | ImportantMemo.md |

**明确定时任务判定标准**（满足任一即使用 feishu-reminder）：
- ✅ 包含具体时间点：`每天 9 点 `、` 每周一 10 点`、` 下午 3 点`
- ✅ 包含相对时间：`30 分钟后 `、`1 小时后 `、`2 天后`
- ✅ 包含具体日期：`3 月 15 日 `、` 下周五`、` 明天上午`
- ✅ 包含周期性：`每天 `、` 每周 `、` 每月`、` 每个工作日`

**待办事项判定标准**（使用 FlashMemo）：
- ✅ 无明确时间：`记得做 X`、`代办：XXX`、`要做的事`
- ✅ 模糊时间：`有空的时候 `、` 找时间`、` 尽快`
- ✅ 计划性：`打算 `、` 准备`、` 计划`

**示例对比**：
```
❌ "每天 9 点提醒我打卡" → FlashMemo
✅ "每天 9 点提醒我打卡" → feishu-reminder（明确时间点）

❌ "30 分钟后通知我开会" → FlashMemo
✅ "30 分钟后通知我开会" → feishu-reminder（相对时间）

❌ "记得买牛奶" → feishu-reminder
✅ "记得买牛奶" → FlashMemo（无明确时间）

❌ "下周一上午 10 点提醒我写周报" → FlashMemo
✅ "下周一上午 10 点提醒我写周报" → feishu-reminder（明确时间）
```

**适用范围**：所有渠道（飞书、Discord、Telegram 等）和所有会话类型

---

### 待办查询逻辑（2026-03-12 确立）
**规则：所有待办事项仅从 FlashMemo ImportantMemo.md 查询**

**查询位置**:
- `~/Documents/FlashMemo/{channel}/{user_id}/ImportantMemo.md`
- 渠道和用户标识根据当前会话自动确定

**查询方式**:
- 直接读取 `ImportantMemo.md` 文件内容
- 筛选标记为"代办"的记录
- 禁止从 MEMORY.md 或其他位置读取待办

**适用范围**: 所有渠道和所有会话类型

### 搜索策略（2026-03-13 确立）

**规则：用户要求搜索内容时，优先使用 tavily 技能**

**触发条件**（满足任一即使用 tavily）：
- ✅ 用户明确说"搜索"、"查一下"、"找一下"
- ✅ 用户问需要联网查询的问题
- ✅ 用户提到"网上看看"、"搜一下"

**执行方式**：
1. 调用 `tavily-search` 技能进行搜索
2. 使用 `node scripts/search.mjs "查询内容"` 命令
3. 默认返回 5-10 条结果
4. 整理结果后回复用户

**配置状态**：
- ✅ 技能已安装：`tavily-search` v1.0.0
- ✅ API Key 已配置：`TAVILY_API_KEY`（在 ~/.bashrc）
- ✅ 位置：`/home/jimmy/.openclaw/workspace/skills/tavily-search/`

**示例**：
```
用户："搜一下 OpenClaw 怎么配置邮箱"
→ 调用 tavily-search 技能
→ 返回搜索结果摘要 + 来源链接
```

**适用范围**：所有渠道（飞书、Discord、Telegram 等）和所有会话类型

---

### 异步任务处理（2026-03-11 确立）

**规则：复杂/长耗时任务必须自动启用子代理异步处理**

**触发条件：**
- 视频/音频下载（B 站、YouTube 等）
- 语音转录（>10 分钟音频）
- 大批量搜索（50+ 网页）
- 数据分析（>100MB 数据集）
- 文件下载（>50MB）
- 批量操作（10+ 文件）
- 任何预计耗时 >1 分钟的任务

**执行方式：**
1. 自动启动 subagent 异步处理
2. 主会话继续响应用户
3. 任务启动时返回任务 ID
4. 完成后主动通知用户
5. 支持随时查询进度

**技术实现：**
- 使用 `sessions_spawn` 启动子代理
- 使用 `subagents list` 查询状态
- 使用 `sessions_history` 查看详细进展
- 每个任务使用 label 标记便于追踪

**示例：**
```
用户："下载这个 B 站视频"
→ 自动启动子代理
→ 返回："任务已启动（ID: xxx），预计 5 分钟"
→ 用户继续聊天
→ 完成后通知："任务完成，文件已保存到 xxx"
```

此规则适用于所有渠道（飞书、Discord、Telegram 等）和所有会话类型。

---

## 个人上下文

### 主人信息
- 时区：Asia/Shanghai
- 语言：中文

### 重要提醒

### 配置偏好
- B 站视频下载目录：`~/Downloads/OpenClaw/bilibili/`
- 语音转录默认模型：Faster Whisper medium
- 自动备份：每天凌晨 2 点，保留 7 天 + 最近 10 个
